import React from 'react'

const Filters = () => {
    return (
        <div>
            <p>Group by: </p>
            <button>Continent</button>
            <button>Language</button>

        </div>
    )
}

export default Filters
